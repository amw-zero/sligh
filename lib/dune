(menhir
 (modules parser)
 (flags --explain))

(ocamllex lexer)

(library
 (name edsl)
 (modules compiler model environment codegen parser lexer util core))

(env
 (dev
  (flags (:standard -w -66))))
