(menhir
 (modules parser)
 (flags --explain))

(ocamllex lexer)

(library
 (name edsl)
 (modules compiler model environment codegen parser lexer util core typescript_syntax))

(env
 (dev
  (flags (:standard -w -66))))
