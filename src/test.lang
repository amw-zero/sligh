schema Application do
  comments: [String]
  
  def add(comment: String)
    comments.push(comment)
  end

  def remove(comment: String)
    comments.delete(comment)
  end

  def update(comment: String)
    comments.update(comment)
  end
end
